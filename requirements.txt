# Python dependencies for ASO Off-Target Screening Pipeline
# Python 3.7+ required

# Core pipeline: No external dependencies required
# (Standard library only: csv, sys, os, pathlib, etc.)

# Optional dependencies for additional features:

# Gemini annotation (optional - only needed for annotate_with_gemini.py)
google-genai>=0.2.0  # For Gemini API integration

# ASO inventory builder (optional - only needed for scripts/build_aso_inventory_final.py)
pandas>=1.3.0  # For Excel file processing
openpyxl>=3.0.0  # For reading .xlsx files (required by pandas)

